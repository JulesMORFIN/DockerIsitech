version: "3"

services:
    minecraft-server:
        build: 
         context: .
         args:
             RAM_AMOUNT: ${RAM_AMOUNT}

        tty: true
        restart: unless-stopped
        volumes:
          - C:\Users\Jules\Desktop\Docker2\test\.server-data:/server
          
        ports:
          - 25565:25565
          - 25575:25575
           
           
        

        container_name:  ${CONTAINER_NAME}        
                
volumes:
   server-data: 
  
            
        